fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0
alembic>=1.14
psycopg[binary]>=3.2
pydantic>=2.10
pydantic-settings>=2.7
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# passlib 1.7.4 (last release, unmaintained) breaks with bcrypt>=4.1 — its
# internal backend self-test relies on behavior that changed there.
bcrypt<4.1
python-multipart>=0.0.20
qrcode[pil]>=8.0
python-barcode>=0.15
reportlab>=4.2
# Used by core/alerts.py for the ERROR webhook. Was previously pulled in
# transitively (Twilio, since removed); now a direct, explicit dependency.
requests>=2.32
stripe>=11.1
email-validator>=2.2
